# Portal: dynamic nav + permission checks (must align with backend ACCESS_ENFORCE_PERMISSIONS)
NEXT_PUBLIC_ACCESS_DYNAMIC_NAV=true
NEXT_PUBLIC_ACCESS_ENFORCE_PERMISSIONS=true

# Backend (set in application.yml / env when running Spring Boot):
# ACCESS_ENFORCE_PERMISSIONS=true
# ACCESS_DYNAMIC_NAV=true
